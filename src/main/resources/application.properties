spring.application.name=transfert-etudiant
server.port=${PORT:8080}

# CONFIGURATION DE LA BASE DE DONNÉES
# Si la variable n'est pas lue par Render, le système basculera AUTOMATIQUEMENT sur ton PostgreSQL de Render plutôt que localhost.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://transfert_db_13py_user:IHKpL5qCbeOSOSSSpDdEGXzjuJGAXH7V@dpg-d8mh8ocvikkc73bs24gg-a/transfert_db_13py}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:transfert_db_13py_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:IHKpL5qCbeOSOSSSpDdEGXzjuJGAXH7V}
spring.datasource.driver-class-name=org.postgresql.Driver

# Force Hibernate à utiliser le dialecte PostgreSQL quoi qu'il arrive
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CONFIGURATION SECURITY & JWT
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS (Prend l'URL finale de ton React, sinon accepte tout par défaut pour éviter les blocages au début)
cors.allowed.origins=${REACT_APP_URL:*}

# Logging
logging.level.com.transfert=DEBUG